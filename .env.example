# Copy to .env and fill in. .env is gitignored.
#
# Password for the static snapshot gate. Front-end only: it keeps the
# dashboard behind a prompt, but the captured JSON under /api/ stays
# fetchable directly, so treat this as "not for casual eyes" rather than
# as access control.
PIP_SNAPSHOT_PASSWORD=
